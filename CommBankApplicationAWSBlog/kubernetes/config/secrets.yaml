apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: commsec
  labels:
    app: commsec
type: Opaque
# NOTE: Update the host with your actual RDS endpoint from: terraform output rds_address
# NOTE: Update the password - retrieve from SSM: aws ssm get-parameter --name "/commsec/production/db-password" --with-decryption
stringData:
  host: "commsec-postgres.cg32wuc800vg.us-east-1.rds.amazonaws.com"
  port: "5432"
  dbname: "commsec"
  username: "commsec_admin"
  password: "REPLACE_WITH_ACTUAL_PASSWORD"
---
# ExternalSecret example for AWS Secrets Manager integration
# Requires External Secrets Operator to be installed
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: db-credentials
#   namespace: commsec
# spec:
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: db-credentials
#     creationPolicy: Owner
#   data:
#     - secretKey: host
#       remoteRef:
#         key: commsec/production/db-credentials
#         property: host
#     - secretKey: port
#       remoteRef:
#         key: commsec/production/db-credentials
#         property: port
#     - secretKey: dbname
#       remoteRef:
#         key: commsec/production/db-credentials
#         property: dbname
#     - secretKey: username
#       remoteRef:
#         key: commsec/production/db-credentials
#         property: username
#     - secretKey: password
#       remoteRef:
#         key: commsec/production/db-credentials
#         property: password
