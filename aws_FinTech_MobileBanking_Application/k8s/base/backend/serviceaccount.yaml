# ============================================================================
# Backend ServiceAccount
# ============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-sa
  labels:
    app: backend
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/fintech-banking-backend-role
automountServiceAccountToken: true
